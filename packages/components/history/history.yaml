#####################################################################
### HISTORY
#####################################################################
history:
  include:
    domains:
      - lock    

    entity_globs:
      - binary_sensor.*_armed
      - binary_sensor.*_door_state
      - binary_sensor.downstairs_ac_leak_detector_*
      - binary_sensor.*_held_open
      - binary_sensor.*_motion_detected
      - binary_sensor.*_occupancy
      - binary_sensor.*_tamper
      - binary_sensor.nursery_*
      - binary_sensor.ring_*
      - sensor.*_avg_temp
      - sensor.*_door_state
      - sensor.*_fan_state
      - sensor.*_temperature
      - sensor.*_window_state
      - sensor.smoke_*


#   Entity listed in entities include: include
#   Otherwise, entity listed in entities exclude: exclude
#   Otherwise, entity matches glob include: include
#   Otherwise, entity matches glob exclude: exclude
#   Otherwise, entity matches domain include: include
#   Otherwise: exclude



