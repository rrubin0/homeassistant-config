#####################################################################
### Script
#####################################################################
script:
  tv_power_off:
    alias: "TV Shutdown"
    sequence:

    - service: switch.turn_off
      entity_id: switch.tv

    - service: switch.turn_off
      entity_id: switch.directv